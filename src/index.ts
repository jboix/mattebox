/**
 * Mattebox — modular adaptive-streaming engine for HLS and DASH.
 *
 * Placeholder export so the toolchain has a real module to build.
 * Stage 01 replaces this with the public surface; the mattebox() factory
 * arrives with the kernel.
 */
export const MATTEBOX = 'mattebox';
