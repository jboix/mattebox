/**
 * Size-limit entry point: "broadcast + audio + drm".
 * Placeholder — imports nothing yet. Populated with real stage imports as stages land,
 * so the budget in config/size-limit.cjs measures an honest composition.
 */
export const preset = 'premium';
